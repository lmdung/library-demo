extends ../layouts/common
block content
  .container
    if wrongLogin
      .alert.alert-danger(role="alert") Đăng nhập sai nhiều quá bạn ơi !!! Chúc may mắn lần sau :D
    h3 Login
    if errors
      each error in errors
        .alert.alert-danger(role="alert")=error
    form(method="POST" class="w-50")
      .form-group
        label(for="email") Email address:
        input(type="text", name="email", id="email", placeholder="Email", class="form-control", value= values? values.email : '')
      .form-group
        label(for="password") Passowrd:
        input(type="password", name="password", id ="password", placeholder="Password", class="form-control")
      button(class="btn btn-primary") Login